// Diff — placeholder until the VCS diff endpoint is designed. Lives in
// plugin-code with the rest of the file/VCS seam.
export function Diff() {
  return <div className="facet-pad">Diff — waiting on endpoint design.</div>;
}
